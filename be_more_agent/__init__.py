"""Be More Agent package.

Modules
-------
config   – constants, paths, load_config()
states   – BotStates enum-like class
prompts  – system prompt construction
actions  – tool / action dispatch (time, search, camera)
audio    – AudioMixin  (recording, playback, TTS, wake-word)
chat     – ChatMixin   (conversation loop, memory, image capture)
gui      – BotGUI      (tkinter UI, animations, main loop) + run_app()
"""
